

export function render(page: PageState, state: State) {

    switch (page.type) {

        case "home":
            return

        case "posts":

            return

        case "detail":
            return

    }
}